class Towel:
    
    def __init__(self, length: int, color):
        self.length = length
        self.color = color
        #return print(f"Length: {length} with color {color}")
        
#T = Towel(45, 'Green')
#print(f'Lenght {T.length} with color {T.color}')

